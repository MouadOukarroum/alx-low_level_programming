#include <stdio.h>

/**
 *main - Entry point
 *printf- print the message to standard output which is the console
 *Return: always 0 (success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
