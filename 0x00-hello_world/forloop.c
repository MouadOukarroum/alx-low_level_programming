#include <stdio.h>


/**
 * main- Entry point
 * For loop: to test something
 * Return: always 0 (Success)
 */

int main(void)
{
	int num = 0;

	for (num < 12;;)
	{
		printf("this is the number: %d", num++);
	}
}
