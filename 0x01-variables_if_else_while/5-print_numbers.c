#include <stdio.h>

/**
 * main - entry point
 * for loop: printing numebrs
 * Return: Always 0 (success)
 */

int main(void)
{
	int i = 48;

	while (i <= 57)
	{
		putchar(i);
		i++;
	}
	putchar('\n');
	return (0);
}
